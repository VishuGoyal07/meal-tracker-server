{
  "scripts": {    
    "clean": "mkdir build",
    "build-babel": "babel -d ./build ./src -s",
    "bbuild": "npm run clean && npm run build-babel",
    "sstart": "npm run bbuild && node ./build/server.js",
    "start": "node ./build/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "body-parser": "^1.20.0",
    "cors": "^2.8.5",
    "express": "^4.18.1",
    "mongo": "^0.1.0",
    "mongodb": "^4.8.0",
    "react-router-dom": "^6.3.0"
  },
  "engines": {
    "node": "16.x"
  },
  "devDependencies": {
    "@babel/cli": "^7.18.9",
    "@babel/core": "^7.18.9",
    "@babel/node": "^7.18.9",
    "@babel/preset-env": "^7.18.9",
    "nodemon": "^2.0.19"
  }
}
